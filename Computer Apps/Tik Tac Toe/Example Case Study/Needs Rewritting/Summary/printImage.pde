//void printImage() {}

/*
//Does not send Array with loaded image, must load single array with all images
//Send index of array referencing the image
PImage[] pic = new PImage[3]; //Array .length is "3"
float scale; //Used for aspect ratio

void printImage (float rectWidth, float rectHeight, int index, float imageX, float imageY, float imageWidth, float imageHeight) {
  scale = imageCalc (rectWidth, rectHeight, imageWidth, imageHeight);
  println("printImage Scale: " + scale);
  image(pic[index], imageX, imageY, imageWidth*scale, imageHeight*scale);
}
*/